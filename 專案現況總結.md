# 專案現況總結 (Project Status Summary)

## 總體狀態 (Overall Status)

本專案已從原型期，成功推進至「**可用產品雛形**」階段。

- **核心功能穩定**: 播放核心、音訊處理、雲端曲庫（Firebase）、以及本機匯入/匯出功能皆已建立且運作穩定。
- **效能問題已解決**: 透過從 V2 資料模型遷移至 **V3 事件模型**，先前在處理複雜樂譜時遇到的效能瓶頸與播放延遲問題已徹底解決。前端 UI (`SheetDisplay.jsx`) 現在能夠在缺少視覺化資料時優雅降級，確保了應用的穩定性。
- **曲庫 manifest 已成為內建曲庫入口**: `public/score-library-manifest.json` 目前提供 47 首 slim JSON 曲目的 metadata；選單顯示名已改由對應 MIDI 檔名產生，而 slim `slug` / `filename` / `localPath` 繼續作為載入與快取識別。

## 目前開發重心與下一步 (Current Focus & Next Steps)

專案開發已進入第二階段，重心從「快速實現功能」轉向「**架構整理與功能深化**」。

### 1. 功能深化 (Feature Deepening)
- **練習模式 (Practice Mode)**: 將產品從單純的播放器，擴展為包含練習功能的工具。
- **格式轉換 (Converter Tools)**: 發展格式轉換能力，特別是針對 MIDI 匯入的處理。

### 2. 資料模型收斂 (Schema Convergence)
- **統一 Score Schema**: 雖然 V2/V3 的 JSON 格式已是基礎，但需要收斂並確立為正式的 schema，避免格式自由擴散。
- **擴充音樂語意**: 在現有基礎上，繼續完善對多音軌 (multi-track)、和弦 (chord)、演奏法 (articulation)、動態 (dynamics) 等元資料的支援。

### 3. 架構與 UI 整理 (Architecture & UI Refactoring)
- **統一資料流**: 整理內部的資料流，確保一致性。
- **UI 元件去重**: 整合控制介面，例如以 `InstrumentSelector` 全面取代舊的音色選擇方式。

### 4. 文件同步 (Documentation Sync)
- **更新文件**: 現有文件（README、交接文件、AI 摘要）與實際功能有明顯落差，急需更新。
- **同步重點**: 必須補上關於 **練習模式 (practice mode)**、**播放頭 (playhead)**、**轉換器 (converter)**、以及 **MIDI 匯入** 的相關說明。

## 已完成的關鍵項目 (Key Completed Items)

- **Firebase 雲端曲庫**: 完成匿名登入、Firestore 曲庫的即時訂閱、樂譜的增刪改查、以及舊資料格式的相容處理。
- **匯入/匯出**: 支援本機單檔/多檔的匯入與上傳，以及將當前樂譜匯出到本機。
- **內建曲庫 manifest**: 已完成 MIDI/slim JSON 曲庫索引產生、公開 slim JSON 複製，以及以 MIDI 檔名作為使用者選曲顯示名稱的整理。

## ESP32 Hina Lyre 韌體

- 新增 `hina_lyre_main.ino`，包含 ESP32 + VS1053B RT-MIDI patch、SD CSV 單次播放、矩陣掃描與按鍵中斷清音。
- 尚未上板編譯或實測；需依實際模組確認腳位、供電、矩陣上拉／二極體及音訊放大器。詳細狀態見 `專案記憶/01_目前系統總覽.md`、`06_樂器發聲與音訊引擎記憶.md`。
