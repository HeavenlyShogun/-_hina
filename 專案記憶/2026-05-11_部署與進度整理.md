# 2026-05-11 部署與進度整理

## 今日完成

- 修正 GitHub Pages 路徑錯位問題
- 建立 GitHub Pages / Firebase Hosting 雙部署相容 base path
- 新增 Firebase 專用 build 流程
- `firebase deploy` 已可透過 `predeploy` 自動使用 Firebase 版 build
- 清理前端譜面，只保留：
  - `surges`
  - `neo-aspect`
- `neo-aspect` 預設 BPM 改為 `180`
- 修正一批前端中文亂碼與壞掉字串

## 目前專案判斷

- 播放邏輯：可用，且已達穩定可彈奏狀態
- 部署結構：可用，GitHub Pages 與 Firebase 已可分流處理
- 主要問題：大譜面造成前端卡頓

## 現在不要優先做的事

- 不要再投入大量時間修播放 scheduler
- 不要先擴充更多譜面功能
- 不要先新增更多大型譜面

## 下一次作業目標

核心任務：

- 樂譜穩定性輸出
- 精簡譜面
- 優化大譜面載入與播放成本

可直接拆成以下工作：

1. 盤點目前 JSON 譜面哪些欄位是播放必要欄位
2. 做一份 slim score 輸出格式
3. 讓前端播放優先讀 slim 格式
4. 保留完整格式只給轉譜/備份/檢查使用
5. 先拿 `surges` 做效能驗證

## 下次開工建議順序

1. 檢查 `surges-midi.json` 的體積、事件數量、可刪 metadata
2. 定義 slim schema
3. 新增 slim 匯出腳本
4. 修改前端播放載入流程
5. 比較優化前後的載入與操作流暢度

## 備註

- `dist` 會依最後一次 build 目標不同而不同，屬正常。
- 發 GitHub Pages 前記得跑 `npm run build:pages`
- 發 Firebase 前可直接跑 `firebase deploy`
