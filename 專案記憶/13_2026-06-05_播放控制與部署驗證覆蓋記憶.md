# 2026-06-05 播放控制與部署驗證覆蓋記憶

最後更新：2026-06-05

本檔為 2026-06-05 最新覆蓋記憶。若舊版記憶未提到播放中鎖定樂器、BPM 更新節流或曲庫來源路徑 metadata，均以本檔為準。預設譜面清單仍承接 `12_2026-06-05_節拍器與預設譜面修正記憶.md` 的 20 首狀態。

## 本次程式狀態

- `src/pages/PianoRoom.jsx` 新增 `isPlaybackActive`，並把 `isBusy || isPlaybackActive` 傳給 `InstrumentSelector`。
- `src/App.jsx` 以目前 `isPlaying` 狀態傳入 `PianoRoom`，播放中會鎖定樂器切換，避免播放排程期間改 tone 造成聲音狀態不一致。
- `src/components/InstrumentSelector.jsx` 的樂器按鈕與 Blend toggle 都沿用 disabled 狀態；disabled toggle 會呈現 wait cursor 與淡化狀態。
- `src/components/ControlPanel.jsx` 的主 BPM 套用改為 `requestAnimationFrame` 節流，滑桿連續拖動時不會在同一 frame 內重複推送 `setBpm`。
- `ControlPanel` unmount 時會取消尚未執行的 BPM animation frame，並保留原有節拍器 timer 與 AudioContext 清理。
- `src/config/scoreLibraryPaths.js` 統一保存預設曲庫根路徑、MIDI 路徑、slim JSON 路徑與預設 slim 檔名。
- `src/data/importableScoreFiles.js` 的 slim metadata 會帶出 `libraryPath` 與 `defaultMidiPath`。
- `src/hooks/useScoreState.js` 的預設 score document 會帶出 `sourcePath`，目前指向 `風物之琴譜/縮小版可匯入譜面/slim-json/surges-slim.json`。

## 驗證紀錄

- `npm run build:firebase` 已通過，輸出到 `dist-fb`。
- `npm run build:pages` 已通過，輸出到 `dist-gh`。
- Firebase preview 已以 `dist-fb` 啟動於 `http://127.0.0.1:4174/`，首頁 HTML 回應 200。
- Firebase preview 的核心資源已確認回應 200：主 JS、React vendor、Tone vendor、主 CSS。
- GitHub Pages preview 已啟動於 `http://localhost:4173/-_hina/`，根路徑 `/` 正確 302 到 `/-_hina/`，base path 首頁回應 200。
- GitHub Pages preview 的核心資源已確認回應 200：主 JS、React vendor、Tone vendor、主 CSS。
- Vite 仍提示 `surges-slim.json` 同時被靜態與動態 import，這是分包警告，不是 build error。
- Vite 仍提示主 chunk 超過 500 kB，這是效能警告；本次未改動分包策略。
- 沙盒內執行 Vite build 仍可能遇到 `commonjs--resolver spawn EPERM`，已用允許的本機程序權限完成實際 build 驗證。

## 部署前注意

- Firebase Hosting 使用 `firebase.json` 的 `dist-fb`，predeploy 會跑 `npm run build:firebase`。
- GitHub Pages 使用 `dist-gh` 與 repo base path `/-_hina/`。
- `dist-fb`、`dist-gh`、`.tmp` 與 `node_modules` 都是本機產物或依賴，不應作為長期記憶或原始碼提交。
