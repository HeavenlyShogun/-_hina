2026-09-19 曲庫擴充與雲端架構演進記憶

階段一與二：曲庫大幅擴充與 Manifest / Service 建立

針對 風物之琴譜/縮小版可匯入譜面/midi/ 新增的 MIDI 曲目，批次補齊可匯入的 hina-slim-score@3.2 slim JSON 譜面，總數達 47 首（共 127,039 個音符）。

建立了完整的 npm 產線指令：scores:slim:library（結合批次轉檔與 Manifest 產出）。

成功產出 public/score-library-manifest.json，精準紀錄 id, slug, bpm, durationTicks, isOfflineCore, storagePath 等雲端擴充欄位，並排除了 Windows UTF-8 BOM 讀取問題。

於 src/services/scoreLibraryService.js 建立核心 API，支援 Manifest 記憶體快取，以及利用 IndexedDB (universe-score-cache) 實作的 fetchScoreBySlug，具備嚴格的快取 Envelope（比對 bytes, durationTicks）避免 stale 資料。

階段三：拔除 Vite 打包依賴，全面換軌非同步架構

徹底移除 import.meta.glob：src/data/importableScoreFiles.js 不再靜態掃描目錄，全面改為呼叫 scoreLibraryService.loadLibraryManifest() 動態獲取。

UI 非同步改造：App.jsx, AppHeader.jsx, PianoRoom.jsx 皆接入 Loading 狀態與錯誤邊界處理。點擊曲目時，全面改走 fetchScoreBySlug 載入實體 JSON。

建置品質大幅提升：成功解決 surges-slim.json 的靜態/動態重複 import 警告，並清除了 47 個龐大的 JSON Chunk，為後續檔案分離鋪平道路。

額外亮點：反轉錄工具 (Slim JSON -> MIDI)

發現部分譜面僅有 Slim JSON 卻遺失原始 MIDI，實作了 scripts/slim-json-to-midi.mjs。

新增 npm run scores:midi:reverse 指令，支援單檔與 --scan-missing 批次掃描。

成功反轉錄並補齊 7 個缺少的 MIDI 檔（如 combined_11.mid, combined_22_mxl.mid 等），確保轉換產線的雙向完整性。

階段四：曲庫選單顯示名對齊 MIDI 檔名

已整理 scripts/generate-score-manifest.mjs，讓 public/score-library-manifest.json 的 title / displayTitle 由對應 MIDI 檔 basename 產生；使用者選曲時會看到 MIDI 檔名。

slim JSON 的 slug, filename, localPath 仍保留作為 fetch/cache 識別，scoreLibraryService.fetchScoreBySlug() 不受顯示名調整影響。

manifest 也會保留 midiFilename / midiLocalPath 供檢查；若目前 MIDI 目錄找不到同 slug 檔案，會使用 slim 內容記錄的 source.midi[0] 或 meta.fileName 作為 MIDI 檔名 fallback。

已重新產出 public/score-library-manifest.json 與 public/score-library/slim-json/，目前 manifest 共 47 首、公開 slim JSON 共 47 檔。

下一步雲端落地建議 (Storage 分離)

目前的「智慧水管」已經架好，JSON 也不再污染前端代碼庫。下一步可以正式將實體檔案搬上雲端：

將 風物之琴譜/縮小版可匯入譜面/slim-json/ 內的檔案上傳至 Firebase Storage (score-library/slim-json/)。

讓 scoreLibraryService 優先請求 Firebase Storage 的 URL（可透過 manifest 的 downloadUrl 或 storagePath 組合），實現完全的伺服器無伺服器 (Serverless) 靜態託管。
