# 2026-05-15 網頁亂碼、節奏調性存檔與部署驗證

## 本次完整版狀態

- 修正網頁可見中文亂碼，範圍包含主畫面 toast、控制面板、譜面編輯/預覽區、Firebase/Firestore 錯誤訊息、預設譜面載入路徑。
- 修正預設 slim 譜面路徑：`風物之琴譜/縮小版可匯入譜面/slim-json/surges-slim.json`。
- 控制面板文字改為穩定繁中：
  - 節奏：BPM、拍號、節拍格線。
  - 音色：音量、殘響、調性、大調/小調/自訂。
  - 寫入按鈕：`寫入目前節奏與調性`。
- 譜面提示區加入 MusicXML 轉換流程：
  1. PDF/JPG/PNG 先用 ACE Studio 轉 MusicXML。
  2. 下載 `.musicxml` 或 `.xml` 後回到本頁匯入。
  3. 檢查 BPM、拍號、調性。
  4. 使用「寫入目前節奏與調性」或直接存檔/匯出。
- MusicXML 參考網址：
  - https://acestudio.ai/pdf-to-musicxml?step=1

## 節奏與調性寫入譜面

- `buildCurrentScoreSnapshot()` 會在存檔/匯出/手動套用時建立目前譜面快照。
- JSON 譜面會更新：
  - `transport.bpm`
  - `transport.timeSigNum`
  - `transport.timeSigDen`
  - `playback.tone`
  - `playback.reverb`
  - `playback.globalKeyOffset`
  - `playback.scaleMode`
  - `playback.accidentals`
- 單一 `playback.tempoMap` 會同步更新 BPM：
  - 支援舊格式 `[[0, bpm]]`
  - 支援物件格式 `{ startTick, bpm, secondsPerTick }`
- 雲端摘要新增保留 `accidentals`，避免升降記號只存在完整文件中。

## 匯入邊界處理

- 空 `.json` 匯入會顯示：`<檔名> 是空的 JSON 檔案。`
- 格式毀損 `.json` 匯入會顯示：`<檔名> 的 JSON 格式無法解析。`
- 匯入 catch 會優先顯示錯誤訊息本身，不再只顯示泛用訊息。
- 已確認相關訊息無亂碼。

## 部署與 MIME 驗證

- Firebase project：`guilty-corn`
- Firebase hosting public dir：`dist-fb`
- Staging channel：`staging`
- Staging URL：
  - https://guilty-corn--staging-7f63ubtj.web.app
- Production Hosting URL：
  - https://guilty-corn.web.app
- Firebase Console：
  - https://console.firebase.google.com/project/guilty-corn/overview
- Staging 到期時間：
  - 2026-05-22 16:07:25

為了可重複驗證 JSON MIME，新增：

- `public/hosting-mime-check.json`

Staging header 驗證結果：

- `/` -> `text/html; charset=utf-8`
- `/assets/index-DdD_O3Kn.js` -> `text/javascript; charset=utf-8`
- `/assets/index-y6YcNerH.css` -> `text/css; charset=utf-8`
- `/hosting-mime-check.json` -> `application/json`

## 瀏覽器驗證

- 本機可用瀏覽器：Microsoft Edge。
- 本機未找到 Firefox 或 Safari 可執行檔，因此 Firefox/Safari 跨瀏覽器項目無法在此機器完成。
- 已用 Edge headless 載入 staging 並產生暫存截圖檢查。
- 截圖顯示中文區域與控制面板基本渲染正常；暫存截圖已清理，避免提交測試產物。

## 上傳路徑

Firebase：

- 專案：`guilty-corn`
- Staging deploy command：
  - `firebase hosting:channel:deploy staging --project guilty-corn --expires 7d`
- Production deploy command：
  - `firebase deploy --only hosting --project guilty-corn`

Git：

- Remote name：`origin`
- Fetch URL：`git@github.com:HeavenlyShogun/-_hina.git`
- Push URL：`git@github.com:HeavenlyShogun/-_hina.git`
- Push command：
  - `git push origin <branch>`

## 驗證指令

- Build：
  - `npm run build`
- Firebase staging deploy：
  - `firebase hosting:channel:deploy staging --project guilty-corn --expires 7d`
- 亂碼掃描：
  - `rg -n "[\\x{0080}-\\x{009F}\\x{E000}-\\x{F8FF}]|嚗|銝|頛|霅|蝮|憸|||||脣|脩|閮|隢|蝛|箇|摮|撌|撱|隤|雿|銋|蝺|靘|瑼|餈|暺|蝭|憟|摰|頧|瘝|蝐|敺|游|頝|閫|畾|擃|憭|撠|芾" src`
