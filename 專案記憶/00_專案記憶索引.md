# Project Hina 專案記憶索引

最後更新：2026-06-03

本目錄用來記錄 Project Hina 的長期工程記憶、作業位置與除錯入口。2026-05-31 起以 `10_整份專案介紹記憶.md` 作為目前最新版的整體介紹；`09_1.1初步完結版記憶.md` 保留為 1.1 初步完結版狀態表，舊主題檔保留為除錯索引。

## 快速入口

- 樂器發聲、無聲、爆音、音色載入：先看 `06_樂器發聲與音訊引擎記憶.md`。
- 美術、版面、響應式、背景與按鈕狀態：先看 `07_美術與介面設計記憶.md`。
- 樂譜 schema、MIDI、MusicXML、Slim JSON、匯入匯出：先看 `03_譜面系統與轉換記憶.md`。
- Firebase Auth、Firestore 曲庫、批次上傳：先看 `04_Firebase與雲端曲庫記憶.md`。
- GitHub Pages、Firebase Hosting、base path 與部署：先看 `05_GitHub與部署記憶.md`。
- 專案根目錄、資料夾、腳本、建置輸出與日常驗證：先看 `08_基礎組成元素與作業位置.md`。
- 1.1 初步完結版的功能、限制、驗證與後續入口：先看 `09_1.1初步完結版記憶.md`。
- 整份專案的最新版定位、架構、工作流、風險與維護原則：先看 `10_整份專案介紹記憶.md`。

## 記憶拆分

- `01_目前系統總覽.md`
  - 專案技術棧、主要資料流、核心模組與目前風險。
- `02_頁面欄位與記憶更新邏輯.md`
  - App 各頁面、欄位、狀態來源與記憶更新規則。
- `03_譜面系統與轉換記憶.md`
  - 樂譜 schema、MIDI/MusicXML/Slim JSON 轉換、匯入匯出與 AI 輔助轉檔。
- `04_Firebase與雲端曲庫記憶.md`
  - Firebase 設定、Auth、Firestore、雲端曲庫與上傳限制。
- `05_GitHub與部署記憶.md`
  - GitHub remote、GitHub Pages、Firebase Hosting、build/deploy 指令。
- `06_樂器發聲與音訊引擎記憶.md`
  - 樂器清單、SoundFont/sample、Web Audio 合成、播放排程與音色除錯。
- `07_美術與介面設計記憶.md`
  - 視覺資產、版面、控制元件、互動狀態與 UI 品質規則。
- `08_基礎組成元素與作業位置.md`
  - 專案目錄、設定檔、腳本、資料資產、測試與日常維護入口。
- `09_1.1初步完結版記憶.md`
  - 2026-05-29 初步完結版狀態，包含 UI、播放、調性、譜面、部署與清理記錄。
- `10_整份專案介紹記憶.md`
  - 2026-05-31 最新整體介紹，包含產品定位、技術骨架、播放、譜面、音色、UI、雲端、部署與維護原則。

## 維護規則

- 新增或修改功能時，同步更新對應主題檔的「作業位置」與「除錯入口」。
- 跨領域改動要同時更新多份記憶。例如：匯入 MIDI 後自動上傳雲端，需更新 `03` 與 `04`。
- 不把臨時 log、build 產物或個人本機狀態寫成長期事實。
- 若發現亂碼、失效路徑、已移除 API，優先修正記憶文件，避免後續依賴錯誤資訊。

## 本次整理狀態

- 2026-06-03 內建可匯入曲庫從 16 首更新為 21 首；新增 `春日影`、`wrong world`、`名無聲`、`星座になれたら`、`Guitar, Loneliness and Blue Planet`，並把本機新增 MIDI 後的 slim-json 轉檔與選單串接流程記錄到 `08`。
- 2026-05-31 新增 `10_整份專案介紹記憶.md` 作為最新版整體專案介紹，並同步整理目前 1.1.0 的工作流、音色、轉檔、Firebase、部署與維護風險。
- 2026-05-29 版本更新至 `1.1.0`，新增鍵盤區播放 / 暫停 / 從頭控制，背景介紹區可手動淡化，調性選單改為大調與自然小調。
- 2026-05-29 譜面讀取收斂為 JSON/Slim JSON 與 MIDI/MusicXML/MXL 轉檔 JSON；舊版鍵盤文字譜、數字譜、keshifu 相關入口與腳本已清理。
- 2026-05-29 `npm.cmd run build:firebase` 已成功，Vite 仍保留 `surges-slim.json` 靜態/動態 import 分包警告。
- 2026-05-29 已將樂器發聲、美術與介面、基礎組成元素拆成獨立記憶檔。
- 2026-05-29 已驗證 `npm.cmd run build:firebase` 與 `npm.cmd run build:pages` 可成功建置。
- 沙盒內執行 Vite build 會遇到 `commonjs--resolver spawn EPERM`，需在允許的本機程序權限下重跑確認；這不是程式碼建置錯誤。
- 目前 Vite 仍提示 `surges-slim.json` 同時被靜態與動態 import，屬效能/分包警告，追蹤位置見 `08`。
